server:
  port: 8086

spring:
  application:
    name: ms-camarero-menu

  datasource:
    url: jdbc:mysql://localhost:3306/dbmenu
    username: root
    password: pass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

inventario:
  service:
    url: http://localhost:8084/inventario

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true